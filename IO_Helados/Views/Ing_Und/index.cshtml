@model IEnumerable<IO_Helados.Models.Ing_Und>

@{
    ViewBag.Title = "index";
}
<div style="padding-top:100px"><h2>Lista de Ing_Und</h2></div>


<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table style="background-color:white" class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.idIngrediente)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.NIngrediente)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.UndDisp)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.idIngrediente)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.NIngrediente)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.UndDisp)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new {  id=item.idIngrediente }) |
            @Html.ActionLink("Details", "Details", new {  id=item.idIngrediente }) |
            @Html.ActionLink("Delete", "Delete", new {  id=item.idIngrediente })
        </td>
    </tr>
}

</table>
